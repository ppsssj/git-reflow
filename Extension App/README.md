# Extension App

This directory is reserved for the browser extension side of git-reflow.

Planned responsibilities:

- sync saved web templates into the extension
- apply customized layouts on live GitHub pages
- manage extension-side storage and GitHub page injection

This area is intentionally left minimal for now while the web frontend is built first.

